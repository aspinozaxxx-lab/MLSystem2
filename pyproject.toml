[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mlsystem2"
version = "0.1.0"
description = "Минимальная архитектура командной строки MLSystem 2.0"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2",
  "PyYAML",
  "mlflow",
  "onnx>=1.16",
  "numpy",
  "Pillow",
  "scipy",
  "boto3",
  "rasterio",
  "shapely",
  "pyproj",
  "fastapi",
  "uvicorn",
  "python-multipart",
  "sqlalchemy>=2",
  "alembic",
  "psycopg[binary]",
]

[project.optional-dependencies]
torch = [
  "onnx>=1.16",
  "torch",
  "torchvision",
  "transformers",
  "segmentation-models-pytorch",
]
dev = [
  "onnx>=1.16",
  "pytest",
  "ruff",
  "httpx",
]

[project.scripts]
mlsystem2-train = "mlsystem2.cli.train:main"
mlsystem2-infer = "mlsystem2.cli.infer:main"
mlsystem2-training-ui-api = "mlsystem2.training_ui_api.api:main"
mlsystem2-training-ui-worker = "mlsystem2.training_ui_api.api:worker_main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = [".", "src"]
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"
line-length = 100
