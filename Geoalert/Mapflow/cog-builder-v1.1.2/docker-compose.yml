version: '3.8'

x-common-minio-variables: &common-minio-variables
  MINIO_HOST: minio
  MINIO_PORT: 9000
  MINIO_ACCESS_KEY: 
  MINIO_SECRET_KEY: #

services:
  app:
    image: cog-builder/test
    container_name: cog_builder_test
    environment:
      <<: *common-minio-variables
      BUCKET_NAME: "healthcheck"
    depends_on:
        - minio

  minio:
    image: minio/minio:RELEASE.2022-06-20T23-13-45Z
    container_name: cog_builder_minio
    command: server --console-address :9001 /data
    environment: *common-minio-variables
